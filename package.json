{
  "name": "third-umpire",
  "version": "0.1.0",
  "description": "Phase 1 MVP: Match Mode + cited Q&A for one competition, free, no offline cache.",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test server/test/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
